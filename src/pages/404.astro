---
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>404 — Sample Not Found</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      html, body {
        margin: 0;
        padding: 0;
        width: 100%;
        height: 100%;
      }

      body {
        background: #0b0e14;
        color: #e6e6e6;
        font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      }

      .screen {
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
      }

      .box {
        max-width: 720px;
        padding: 2rem;
      }

      h1 {
        font-size: 3rem;
        margin-bottom: 1rem;
      }

      .code {
        color: #7aa2f7;
      }

      .dim {
        color: #9aa4b2;
      }

      a {
        display: inline-block;
        margin-top: 2rem;
        color: #7aa2f7;
        text-decoration: none;
        border: 1px solid #7aa2f7;
        padding: 0.6rem 1.2rem;
        border-radius: 6px;
      }

      a:hover {
        background: #7aa2f7;
        color: #0b0e14;
      }
    </style>
  </head>
  <body>
    <div class="screen">
      <div class="box">
        <h1>☠️ 404 — Sample Not Found</h1>

        <p class="code">0xC0000005 — ACCESS_VIOLATION</p>

        <p class="dim">
          The binary you are looking for has been deleted,<br />
          unpacked, or self-destructed inside a sandbox.
        </p>

        <p class="dim">
          Maybe the URL is wrong. Maybe the malware didn’t want to be analyzed.
        </p>

        <a href="/">↩ Return to lab</a>
      </div>
    </div>
  </body>
</html>
